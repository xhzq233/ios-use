#import <UIKit/UIKit.h>

// Decoration only: never insert these views into the App's UIKit hierarchy.
FOUNDATION_EXPORT UIImage *IOSUsePlayStatusBarImage(BOOL lightContent);
FOUNDATION_EXPORT BOOL IOSUsePlayStatusBarUsesLightContent(void);
